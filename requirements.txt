requests>=2.31
pydantic>=2.5
python-dotenv>=1.0
pytest>=8.0

# Phase 2 (Path B: unstructured retrieval)
beautifulsoup4>=4.12
lxml>=5.0
rank-bm25>=0.2
qdrant-client>=1.9
scikit-learn>=1.4
numpy>=1.26

# Phase 6 (semantic retrieval): bi-encoder embeddings + cross-encoder reranking.
# Pulls in torch. Set RETRIEVAL_PROFILE=lexical to run the TF-IDF stack instead.
sentence-transformers>=3.0
